## 2026-08-11 智能重试中间层 v1.1 实现

### 完成内容
- 基于需求文档 v1.1，使用 Go 实现了完整的智能重试中间层
- 7 个核心模块全部实现：config, rule, retry, proxy, logger, middleware, metrics
- 18 个测试全部通过（单元 + 集成）
- Benchmark: 日志 toggle 关闭 1.58ns/op，退避计算 16.9ns/op

### 集成测试覆盖
- AC-01: JSONPath $.code==700 短路重试（3 次重试后 Agent 收到成功结果）
- AC-02: HTTP 429 指数退避重试（5 次上限，实际 3 次后成功）
- AC-03: skip_retry=true 直接返回错误不重试

### 遇到的问题及修复
1. Prometheus promauto 重复注册 → 改用自定义 Registry
2. httptest.NewRequest 的 RequestURI 冲突 → 用 NewRequestWithContext 重建
3. ojg 库不需要 /v2 路径
4. jp.Parse 需要 []byte 参数

## 2026-08-11 Web 管理界面实现

### 技术选型
- 前端: React 18 + Vite + Ant Design 5 + TypeScript
- 后端: Go internal/admin/ REST API + go:embed 内嵌
- 管理端口: :15723

### 完成内容
- 5 个页面: Dashboard / Rules / RuleEditor / Logging / Proxy
- 6 个 REST API: GET/PUT config, CRUD rules, PUT logging, GET status
- config/watcher.go 新增 Path() + SaveAndReload() (暂停监听→写YAML→恢复监听)
- 所有 config 结构体添加 json tag (snake_case) 与前端对齐
- 前端构建产物通过 go:embed 内嵌到二进制

### 遇到的问题及修复
1. cp -r 造成 dist/ 双层嵌套 → 重新正确复制
2. go build 缓存导致旧二进制无 json tag → go clean -cache
3. 端口冲突残留进程 → PowerShell Stop-Process 清理
